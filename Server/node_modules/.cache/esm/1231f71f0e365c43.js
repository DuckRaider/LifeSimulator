let Bank;_0e3‍.x([["getAllBanks",()=>getAllBanks],["createBank",()=>createBank],["getBankById",()=>getBankById]]);_0e3‍.w("../Models/Bank",[["Bank",["Bank"],function(v){Bank=v}]]);

       const getAllBanks = async (req, res) => {
    const banks = await Bank.findAll();

    if(banks) res.status(200).json(banks);
    else{
        res.status(500).send('Internal Server Error');
    }
};

       const createBank = async (req, res) => {
    const { name, balance} = req.body;

    const newBank = await Bank.create({
        name,
        balance
    });

    if(newBank) res.status(201).json(newBank);
    else{
        res.status(500).send('Internal Server Error');
    }
};

       const getBankById = async (req, res) => {
    const users = await Bank.findAll({
        where:{
            id:req.query["id"]
        }
    });

    if(users) res.status(200).json(users);
    else{
        res.status(500).send('Internal Server Error');
    }
};