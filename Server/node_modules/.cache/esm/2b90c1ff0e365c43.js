let User,generateAccessToken;_0e3‍.x([["register",()=>register]]);_0e3‍.w("../Models/User",[["User",["User"],function(v){User=v}]]);_0e3‍.w("../utils/jwtGenerator",[["generateAccessToken",["generateAccessToken"],function(v){generateAccessToken=v}]]);


// Controller function to create a new user
       const register = async (req, res) => {
    const { username, email, password} = req.body;

    const newUser = await User.create({
        username,
        email,
        password
        /* other fields */
    });


    if(newUser){
        const token = generateAccessToken(newUser.dataValues);
        res.status(201).json(token);
    }
    else{
        res.status(500).send('Internal Server Error');
    }
};