let User,generateAccessToken;_03a‍.x([["register",()=>register]]);_03a‍.w("../Models/User",[["User",["User"],function(v){User=v}]]);_03a‍.w("../utils/jwtGenerator",[["generateAccessToken",["generateAccessToken"],function(v){generateAccessToken=v}]]);


// Controller function to create a new user
       const register = async (req, res) => {
    const { username, email, password} = req.body;

    const newUser = await User.create({
        username,
        email,
        password
        /* other fields */
    });


    if(newUser){
        const token = generateAccessToken(newUser.dataValues);
        res.status(201).json(token);
    }
    else{
        _03a‍.g.console.error('Error creating user:', error);
        res.status(500).send('Internal Server Error');
    }
};